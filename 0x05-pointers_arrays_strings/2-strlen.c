#include"main.h"
/**
 * _strlen - length of string.
 * @s: variable
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int _strlen(char *s)
{
int x = strlen(s);
return (x);
}
